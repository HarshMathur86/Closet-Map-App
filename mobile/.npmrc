legacy-peer-deps=true
# image-size advisory (GHSA-w3rx-r6r6-pgpr, GHSA-5p2g-fcmc-qvqq) marks <=2.0.2
# as vulnerable but 2.0.2 IS the latest release — no patch exists yet upstream.
# metro (dev-only bundler) depends on image-size. This is NOT a production risk.
# Revisit when image-size >=2.0.3 is published.
audit-level=critical
